"use strict";
// Types
let framework = "Angular";
const isProduction = true;
const PORT = 3000;
const styles = ['./css/style.css'];
const sayHello = (name) => `Hello ${name}`;
const algo = sayHello('Wakanda por siempre');
console.log(algo);
